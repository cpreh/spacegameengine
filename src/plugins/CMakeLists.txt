IF (NOT DEFINED ${DISABLE_OPENGL})
	ADD_SUBDIRECTORY (ogl)
ENDIF ()

IF (NOT DEFINED ${DISABLE_XINPUT})
	ADD_SUBDIRECTORY (xinput)
ENDIF ()

IF (NOT DEFINED ${DISABLE_TRUETYPE})
	ADD_SUBDIRECTORY (freetype)
ENDIF ()

IF (NOT DEFINED ${DISABLE_DEVIL})
	ADD_SUBDIRECTORY (devil)
ENDIF ()

IF (NOT DEFINED ${DISABLE_OPENAL})
	ADD_SUBDIRECTORY (openal_player)
ENDIF ()

IF (NOT DEFINED ${DISABLE_VORBIS})
	ADD_SUBDIRECTORY (vorbis)
ENDIF ()

IF (NOT DEFINED ${DISABLE_WAVE})
	ADD_SUBDIRECTORY (wave)
ENDIF ()
