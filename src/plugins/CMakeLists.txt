IF (NOT ${DISABLE_OPENGL})
	ADD_SUBDIRECTORY (ogl)
ENDIF ()

IF (NOT ${DISABLE_XINPUT})
	ADD_SUBDIRECTORY (xinput)
ENDIF ()

IF (NOT ${DISABLE_TRUETYPE})
	ADD_SUBDIRECTORY (freetype)
ENDIF ()

IF (NOT ${DISABLE_DEVIL})
	ADD_SUBDIRECTORY (devil)
ENDIF ()

IF (NOT ${DISABLE_OPENAL})
	ADD_SUBDIRECTORY (openal_player)
ENDIF ()

IF (NOT ${DISABLE_VORBIS})
	ADD_SUBDIRECTORY (vorbis)
ENDIF ()

IF (NOT ${DISABLE_WAVE})
	ADD_SUBDIRECTORY (wave)
ENDIF ()
