# - Try to find SGE
#
# This module defines the following variables
#
#	SGE_FOUND
#	SGE_INCLUDE_DIRS
#	SGE_CORE_LIBRARY
#

FIND_PATH(
	SGE_INCLUDE_DIRS
	NAMES sge/config.hpp
)

FIND_LIBRARY(
	SGE_CORE_LIBRARY
	NAMES sgecore
)

FOREACH(
	COMPONENT
	${SGE_FIND_COMPONENTS}
)
	FIND_LIBRARY(
		SGE_${COMPONENT}_LIBRARY
		NAMES sge${COMPONENT}
	)

	IF(
		NOT SGE_${COMPONENT}_LIBRARY
	)
		MESSAGE(
			FATAL_ERROR
			"${COMPONENT} not found!"
		)
	ENDIF()

	MARK_AS_ADVANCED(
		SGE_${COMPONENT}_LIBRARY
	)
ENDFOREACH()

INCLUDE(FindPackageHandleStandardArgs)

FIND_PACKAGE_HANDLE_STANDARD_ARGS(
	SGE
	DEFAULT_MSG
	SGE_INCLUDE_DIRS
	SGE_CORE_LIBRARY
)

MARK_AS_ADVANCED(
	SGE_INCLUDE_DIRS
	SGE_CORE_LIBRARY
)
