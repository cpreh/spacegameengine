add_sge_console_example(
	opencl_simple
	SGE_DEPS
		sgelog
		sgeopencl
		sgerenderer
		sgesystems
		sgeviewport
		sgewindow
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		fcppt_log_interface
		${Boost_FILESYSTEM_LIBRARIES}
)

add_sge_console_example(
	opencl_syntax_checker
	SGE_DEPS
		sgelog
		sgeopencl
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		${fcppt_log_TARGET}
		${fcppt_filesystem_TARGET}
)

add_sge_console_example(
	opencl_texture_read
	SGE_DEPS
		sgeconfig
		sgeimage2d
		sgemedia
		sgeopencl
		sgesystems
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
)
