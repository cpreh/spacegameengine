add_sge_example(
	cg_parameters
	SGE_DEPS
		sgecg
		sgeimagecolor
		sgerenderer
		sgerenderercg
		sgesystems
		sgeviewport
		sgewindow
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		${Boost_FILESYSTEM_LIBRARIES}
)

add_sge_example(
	cg_pointsprites
	SGE_DEPS
		sgecg
		sgeconfig
		sgeimagecolor
		sgemedia_interface
		sgerenderer
		sgerenderercg
		sgesystems
		sgeviewport
		sgewindow
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		${Boost_FILESYSTEM_LIBRARIES}
)

add_sge_example(
	cg_simple
	SGE_DEPS
		sgecg
		sgeimagecolor
		sgerenderer
		sgerenderercg
		sgesystems
		sgeviewport
		sgewindow
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		${Boost_FILESYSTEM_LIBRARIES}
)

add_sge_example(
	cg_texture
	SGE_DEPS
		sgecg
		sgeconfig
		sgeimagecolor
		sgemedia_interface
		sgerenderer
		sgerenderercg
		sgesystems
		sgeviewport
		sgewindow
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		${Boost_FILESYSTEM_LIBRARIES}
)
