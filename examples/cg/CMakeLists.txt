add_sge_example(
	cg/parameters
	SGE_DEPS
		sgecg
		sgeimagecolor
		sgerenderer
		sgerenderercg
		sgesystems
		sgeviewport
		sgewindow
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		${Boost_FILESYSTEM_LIBRARIES}
		${Boost_SYSTEM_LIBRARIES}
)

add_sge_example(
	cg/pointsprites
	SGE_DEPS
		sgecg
		sgeconfig
		sgeimagecolor
		sgemedia_interface
		sgerenderer
		sgerenderercg
		sgesystems
		sgeviewport
		sgewindow
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		${Boost_FILESYSTEM_LIBRARIES}
		${Boost_SYSTEM_LIBRARIES}
)

add_sge_example(
	cg/simple
	SGE_DEPS
		sgecg
		sgeimagecolor
		sgerenderer
		sgerenderercg
		sgesystems
		sgeviewport
		sgewindow
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		${Boost_FILESYSTEM_LIBRARIES}
		${Boost_SYSTEM_LIBRARIES}
)

add_sge_example(
	cg/texture
	SGE_DEPS
		sgecg
		sgeconfig
		sgeimagecolor
		sgemedia_interface
		sgerenderer
		sgerenderercg
		sgesystems
		sgeviewport
		sgewindow
	ADDITIONAL_DEPS
		${fcppt_core_TARGET}
		${Boost_FILESYSTEM_LIBRARIES}
		${Boost_SYSTEM_LIBRARIES}
)
