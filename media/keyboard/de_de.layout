include defaultkeynames.include
include defaultmodifiers.include
include defaultmodifiers-usealtgr.include

{keynames
	console = ^
}

{modifiers
	{names
		lshift   = Shift (Links)
		rshift   = Shift (Rechts)
		caps     = Caps Lock
		lalt     = Alt (Links)
		ralt     = Alt (Rechts)
		ctrl     = Strg
		lctrl    = Strg (Links)
        	rctrl    = Strg (Rechts)
		lwin     = Win (Links)
		rwin     = Win (Rechts)
		num      = Num Lock
		scroll   = Rollen
	}
}

mapping {
}
