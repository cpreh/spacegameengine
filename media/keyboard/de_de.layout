include defaultkeynames.include
include defaultmodifiers.include
include defaultmodifiers-usealtgr.include
include defaultcharactermapping.include

{keynames
	console    = ^
	return     = Eingabe
	numreturn  = Eingabe (Num)
	numdecimal = , (Num)
}

{modifiers
	{names
		lshift   = Shift (Links)
		rshift   = Shift (Rechts)
		caps     = Caps Lock
		lalt     = Alt (Links)
		ralt     = Alt (Rechts)
		ctrl     = Strg
		lctrl    = Strg (Links)
        	rctrl    = Strg (Rechts)
		lwin     = Win (Links)
		rwin     = Win (Rechts)
		scroll   = Rollen
	}
}

{mapping

}
